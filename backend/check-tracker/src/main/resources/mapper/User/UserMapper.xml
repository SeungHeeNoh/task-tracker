<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hohohehe.checktracker.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.hohohehe.checktracker.domain.User">
        <id property="userSeq" column="user_seq"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="password" column="password"/>
        <result property="createdAt" column="created_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="modifiedAt" column="modified_at"/>
        <result property="modifiedBy" column="modified_by"/>
        <result property="groupSeq" column="group_seq"/>
    </resultMap>

    <select id="findByUserId" parameterType="string" resultMap="UserResultMap">
        SELECT
            user_seq,
            user_id,
            user_name,
            password,
            created_at,
            created_by,
            modified_at,
            modified_by,
            group_seq
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="save" parameterType="com.hohohehe.checktracker.domain.User">
        INSERT INTO users
            (
             user_id,
             user_name,
             password,
             created_at,
             created_by,
             modified_at,
             modified_by,
             group_seq
            )
        VALUES (
                #{userId},
                #{userName},
                #{password},
                now(),
                #{createdBy},
                now(),
                #{modifiedBy},
                #{groupSeq}
               )
    </insert>
</mapper>